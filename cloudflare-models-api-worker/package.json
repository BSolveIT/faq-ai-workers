{
	"name": "cloudflare-models-api-worker",
	"version": "1.0.0",
	"private": true,
	"description": "Comprehensive Cloudflare Worker that dynamically fetches all text generation models from the official Cloudflare API with enhanced metadata",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:watch": "vitest --watch",
		"type-check": "tsc --noEmit",
		"preview": "wrangler dev --remote"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.20",
		"@cloudflare/workers-types": "^4.20250115.0",
		"vitest": "^3.0.9",
		"vitest-environment-miniflare": "^2.14.4",
		"wrangler": "^4.19.2"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"keywords": [
		"cloudflare-workers",
		"ai",
		"models",
		"text-generation",
		"api",
		"metadata",
		"dynamic-fetch"
	]
}
